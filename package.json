{
  "name": "gallery-pulse",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "cheerio": "1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
